spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/space_flight_news
    username: postgres
    password: postgres
  jpa:
    show-sql: true
#    hibernate:
#      ddl-auto: update
logging.level.org.hibernate.SQL: DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder: TRACE
